// main ===================================================== //
// fucntions
type getLengthRow = (element?: HTMLElement | null) => number
// objects
interface CSSStyleValue {
    value: number,
    unit: string
}

// export =================================================== //
export type { getLengthRow, CSSStyleValue };