// main ===================================================== //
type inRange = (value: number, min: number, max: number) => number

// export =================================================== //
export type { inRange };