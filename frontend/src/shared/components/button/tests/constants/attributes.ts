// import =================================================== //
import { HTMLAttributes } from "react";

// main ===================================================== //
export const attributes: HTMLAttributes<HTMLButtonElement> = {
    id: "button",
    onClick: () => "Hello world!",
};